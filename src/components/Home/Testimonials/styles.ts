import { createStyles } from '@mantine/core'

export const useStyles = createStyles(() => ({
  testimonialsWrapper: {
    minHeight: '100vh',
    display: 'flex',
    alignItems: 'center',
    overflow: 'hidden',
    '.card3dWrapper': {
      height: '100%',
      minHeight: 466,
    },
  },
}))
